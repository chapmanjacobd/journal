> While iʼm in su mode i can’t even use apt

It's because Termux is different. normal users can use apt in Termux. root access is for other things. Running Termux's apt with sudo will break stuff for the normal user mode
