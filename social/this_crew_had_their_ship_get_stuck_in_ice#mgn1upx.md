> for us all

really it only protects their own privacy (and even then only to some extent: _big brother_ could tie that reddit username to their google account via other means to effectively make the same connection that the `si` parameter is doing)

**For everyone else** YouTube already has the HTTP `referer` metadata which tells YouTube that we came from this specific reddit page
