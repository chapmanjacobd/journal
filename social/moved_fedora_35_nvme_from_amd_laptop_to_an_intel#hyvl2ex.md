(it plays fine) but I still see no audio devices in KDE even after running 

     sudo dnf swap wireplumber pipewire-media-session
     sudo dnf install pulseaudio --allowerasing

and rebooting twice
